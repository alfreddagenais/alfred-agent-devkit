# Cursor adapter

| Source | Destination |
|--------|-------------|
| `content/CLAUDE.md.starter` | `CLAUDE.md` only if missing (CLI opt-in) |
| `content/reference.md` | `.cursor/docs/project-context-contract.md` (optional) |

Do not overwrite an existing `CLAUDE.md`.
