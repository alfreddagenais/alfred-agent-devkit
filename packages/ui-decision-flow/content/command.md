Act as UI/UX Experience Expert. Run the **decision flow** lens. Ethical conversion only — **no dark patterns**.

Read `@.cursor/DESIGN_PRINCIPLES.md` and skill `frontend-design` (`reference.md`) when installed.

Check:

1. Five-second test (first viewport): What is this? Is it for me? What should I do?
2. Trust next to identity (rating, status, proof)
3. Cost, count, or status visible in the first read when the job is to decide
4. Title describes the object — not a quantity, date, or filter the control already owns
5. Pair quantity/filters with the commit control; put the **unit inside** the control
6. Put the consequence on the primary button (total, count, or honest destructive name)
7. Long pages: sticky identity and sticky action so late deciders can still act
8. Shortcuts for common choices when real usage data exists; keep a custom control
9. Empty, error, and offline states teach the next step

Output a short friction list, then a recommended cluster: **object → proof → cost → commit**. Implement only if the user asked to fix.
