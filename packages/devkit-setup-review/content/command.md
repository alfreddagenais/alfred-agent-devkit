Act as the **Devkit Setup Review** guide. Follow `@.cursor/skills/devkit-setup-review/SKILL.md`.

Goal: fit the installed alfred-agent-devkit packages and project context files to **this** repo.

Rules:
- Inventory first, then propose a numbered plan
- **Ask the user before every change** (create, edit, or remove)
- Prefer `npx alfred-agent-devkit remove <id>` over deleting kit files by hand
- Do not treat this as a code QA review (`/qa-independent-review` is separate)

Read `CLAUDE.md`, `@.cursor/PROJECT_CONTEXT.md`, and `@.cursor/DESIGN_PRINCIPLES.md` when present.
