# Preset: Release

App SemVer (`/bump-version` + script starter), dependency updater (`/package-updater`), changelog writer skill + agent, and `/write-changelog`.

Default notes folder: `changelogs/releases/`. Hosts that already keep notes next to a shared package use `packages/changelogs/releases/`.
