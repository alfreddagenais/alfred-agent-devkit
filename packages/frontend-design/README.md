# Frontend Design

Generic frontend design skill. Pair with UI commands and the project’s `DESIGN_PRINCIPLES.md`.

`content/reference.md` is the product-UI craft playbook (system vs screenshot, failure classes, surface fit, finish gate).
