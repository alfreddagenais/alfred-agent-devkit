# Package Updater

Skill + companion agent + `/package-updater` for safe dependency upgrades.

Applies patch/minor first. Defers majors with a report. App SemVer stays with `bump-version`.

Pair with `preset-release`.
