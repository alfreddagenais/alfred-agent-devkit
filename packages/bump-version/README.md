# Bump Version

Skill + companion agent + `/bump-version` for app SemVer (not dependency updates).

Prefers `./scripts/bump-version.sh` when present. `init` writes that script only if the host does not already have one.

Pair with `package-updater`, `changelog-writer`, and `preset-release`.
