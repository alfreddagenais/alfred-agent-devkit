# Changelog Writer

Skill + companion agent for short user-facing release notes (minor/major only).

Default notes folder: `changelogs/releases/`. If the host already has `packages/changelogs/releases/`, that path wins.

Pair with the `write-changelog` command (`/write-changelog`) via `preset-release`.
